services:
  - type: web
    name: Siet78tavos
    env: static
    buildCommand: "npm run build"  # O "yarn build" si usas yarn
    outputDirectory: "build"
    redirects:
      - source: /.*
        destination: /index.html
        status: 200
