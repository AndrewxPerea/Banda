services:
  - type: web
    name: siete-octavos
    env: static
    buildCommand: "npm run build"  # o "yarn build" si usas yarn
    outputDirectory: "build"
    redirects:
      - source: /.*
        destination: /index.html
        status: 200
